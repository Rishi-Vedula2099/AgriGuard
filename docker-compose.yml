version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: agriguard-pg
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=agriguard
    ports:
      - "5433:5432"
    volumes:
      - ./storage/postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: agriguard-redis
    ports:
      - "6379:6379"
    volumes:
      - ./storage/redis_data:/data

  qdrant:
    image: qdrant/qdrant:latest
    container_name: agriguard-qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./storage/qdrant_data:/qdrant/storage

  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/agriguard
      - REDIS_URL=redis://redis:6379/0
      - QDRANT_URL=http://qdrant:6333
      - ML_SERVICE_URL=http://ml-service:8001
      - AI_ASSISTANT_URL=http://ai-assistant:8002
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - ./storage:/app/storage
    depends_on:
      - db
      - redis
      - qdrant
      - ml-service
      - ai-assistant

  ml-service:
    build:
      context: .
      dockerfile: apps/ml-service/Dockerfile
    ports:
      - "8001:8001"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - STORAGE_DIR=/app/storage
    volumes:
      - ./storage:/app/storage

  ai-assistant:
    build:
      context: .
      dockerfile: apps/ai-assistant/Dockerfile
    ports:
      - "8002:8002"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - STORAGE_DIR=/app/storage
      - QDRANT_URL=http://qdrant:6333
    volumes:
      - ./storage:/app/storage
    depends_on:
      - qdrant

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    volumes:
      - ./storage:/app/storage
